<div class="card bg-light" style="width: 30rem;">
  <div class="card-body">
    <h4 class="card-title">Add <%= params['type'] %></h4>
    <%= render 'form', accounting: @accounting %>
  </div>
</div>

<%#= link_to 'Back', accountings_path %>
